import { BLACK, BLUE_LIGHT, WHITE, } from "./colors";

const lighTheme = {
  colors: {
    title: BLACK,
    primary: BLUE_LIGHT,
    secondary: WHITE,
    white: WHITE,
  }
};
export default lighTheme;